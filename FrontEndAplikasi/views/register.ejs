<body>
  <%- include ("partials/messages"); %>
  <div class="main">
    <div class="signup">
      <form action="/users/register" method="POST"> <label>Sign up</label>
        <input type="name" id="name" name="name" class="form-control" placeholder="Username" value="<%= typeof name != 'undefined' ? name : '' %>" required="">
        <input type="email" id="email" name="email" class="form-control" placeholder="Email" value="<%= typeof email != 'undefined' ? email : '' %>" required="">
        <input type="password" id="password" name="password" class="form-control" placeholder="Create Password" value="<%= typeof password != 'undefined' ? password : '' %>" required="">
        <input type="password" id="password2" name="password2" class="form-control" placeholder="Confirm Password" value="<%= typeof password2 != 'undefined' ? password2 : '' %>" required="">
        <button>Sign up</button>
      </form>
    </div>
  </div>
</body>
</html>